# Rust / Anchor build output (contains the program keypair)
target/
**/*.rs.bk

# Local validator state
test-ledger/
# ⚠ The rehearsal's validator ledger. Same thing as test-ledger, different name — and its
# absence here put 20 files of rocksdb into a commit (18 Sep).
rehearse-ledger/
validator.log
*.log

# Node
node_modules/

# Generated by differential.test.mjs
vectors.json

.DS_Store

# Indexer state — a rebuildable cache of sale addresses, not source
indexer/*.db
indexer/*.db-wal
indexer/*.db-shm

# Demo metadata for the local launchpad, served by the dev server
web/public/demo/
fixtures/usdc-authority.json

# Program identity + upgrade authority keypairs. NEVER commit these.
keys/
